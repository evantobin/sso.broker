{
  "name": "sso-broker",
  "version": "0.1.0",
  "description": "Cloudflare Pages/Workers OIDC client and provider for sso.broker",
  "main": "worker.js",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "ejs": "^3.1.9"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
